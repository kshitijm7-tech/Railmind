"""API schema package (E07 addition).

The existing backend keeps its request/response models in
``app/api/models.py``; the E07 engine-boundary schemas live in this package
to keep the new surface explicit without touching existing files.
"""
