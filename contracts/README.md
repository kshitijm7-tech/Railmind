# Shared Domain Contracts

This directory mirrors the canonical shared domain contracts.
Currently, while there is no Python backend, the canonical contracts are stored within the `frontend/contracts/` directory to keep them within the TypeScript compilation boundary.
Once the Python backend is introduced, the canonical definitions will be moved here, and both frontend and backend will consume them.
